# Pipeline Reference

This directory is reserved for a reference to `impresso-pipelines`, which contains the older `NewsAgenciesPipeline`.

The new Hugging Face pipeline should adapt that implementation into `hf_model/pipeline.py` while remaining self-contained in the published model repo.
