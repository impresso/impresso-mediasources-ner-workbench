# Training Submodule

This directory is reserved for the future training-code submodule:

```text
training/newsagency-radiostation-modernbert-classifier/
```

The submodule should contain ModernBERT-only token-classification training, evaluation, prediction, and JSONL span alignment code.
