# Hugging Face Submodules

This directory is reserved for Hugging Face repository submodules:

- `newsagency-radiostation-dataset/`
- `newsagency-radiostation-testset/`
- `mmbert-impresso-mediasources-ner/`

Do not edit cards or pipeline code only inside `hub/*`. Source copies live in `hf_dataset/`, `hf_testset/`, and `hf_model/`.
